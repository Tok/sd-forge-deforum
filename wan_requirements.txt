# Unified WAN-compatible dependencies for webui-forge
# Updated URLs and version constraints for 2025

# Core dependencies for Open-Sora support
diffusers>=0.29.0,<0.35.0
transformers>=4.44.0,<4.50.0
accelerate>=0.34.0,<0.40.0

# Essential ML dependencies
torch>=2.0.0
torchvision>=0.15.0
einops>=0.8.0

# Image and video processing
Pillow>=9.0.0
opencv-python>=4.8.0
imageio>=2.34.0
imageio-ffmpeg>=0.5.1

# Utilities
numpy>=1.21.0,<2.0.0
safetensors>=0.4.0
huggingface-hub>=0.20.0

# Optional performance improvements
xformers>=0.0.27; sys_platform != "darwin"
flash-attn>=2.0.0; sys_platform != "darwin"

# Audio processing (for video generation)
librosa>=0.10.0
soundfile>=0.12.0

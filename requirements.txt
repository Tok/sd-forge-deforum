numexpr
matplotlib
pandas
av
pims
imageio_ffmpeg
rich
gdown
easydict
git+https://github.com/huggingface/diffusers.git
transformers>=4.36.0,<5.0.0
accelerate>=0.25.0,<2.0.0

# Depth Anything V3 (optional - Phase 1/2/3 integration)
# depth-anything-3>=0.1.0
# xformers>=0.0.20  # Required by DA3
# gsplat>=0.1.0  # Required by DA3 Phase 3 (3D Gaussian Splatting)
# Note: Uncomment to enable DA3 support. DA2 remains default.

# Audio sync dependencies
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.10.0
plotly>=5.14.0

# Zero-HITL audio generation (Qwen's slopcore engine)
# stable-audio-tools>=0.1.0  # Python 3.12 incompatible
# Using Meta MusicGen via transformers instead (already in requirements)
# facebook/musicgen-small (300M) via transformers pipeline